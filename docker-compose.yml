services:
  redis:
    image: valkey/valkey:7-alpine
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  streamer:
    build:
      context: .
    container_name: streamer
    ports:
      - "8080:8080"
    environment:
      STREAMER_PORT: 8080
      STREAMER_LOG_LEVEL: info
      STREAMER_BASE_URL: http://localhost:8080
      STREAMER_REDIS_URL: redis://redis:6379
    env_file: api/.env
    depends_on:
      - redis

volumes:
  redis_data:
